/// <reference types="bun-types" />

import { describe, test } from 'bun:test';

describe('commands installer', () => {
  test('placeholder test', () => {
    // Placeholder test since all omos-related tests were removed
  });
});
